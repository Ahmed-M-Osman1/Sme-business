{
  "name": "@shory/db",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "pnpm exec tsup src/index.ts --format esm --external drizzle-orm --external postgres --outDir dist",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:seed": "tsx src/seed.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.39",
    "postgres": "^3"
  },
  "devDependencies": {
    "@shory/typescript-config": "workspace:*",
    "drizzle-kit": "^0.30",
    "tsup": "^8.5.1",
    "tsx": "^4",
    "typescript": "^5"
  }
}
